<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "SRE Devlog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Google Fonts - Fira Code %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@300..700&family=Noto+Sans+JP:wght@100..900&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "css-zero/reset", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/variables", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "css-zero/utilities", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "typography", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "header", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "footer", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "articles", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="header-layout">
    <header id="header" data-controller="theme">
      <div class="header-inner">
        <h1 class="site-title">
          <%= link_to "SRE Devlog", root_path, class: "site-title-link font-accent" %>
        </h1>
        <div class="status-badge font-accent">
          <span class="status-indicator"></span>
          Online
        </div>
        <span class="header-spacer"></span>
        <button class="theme-toggle" data-action="click->theme#toggle" data-theme-target="icon">
          🌙
        </button>
      </div>
    </header>

    <main id="main">
      <% if notice.present? %>
      <div popover="manual" class="flash" style="--flash-position: 5rem" data-controller="flash" data-action="animationend->flash#hide" role="alert">
          <div class="flash__content"><%= notice %></div>
        </div>
      <% end %>
      <div class="container">
        <%= yield %>
      </div>
    </main>
    <footer id="footer">
      <div class="container flex justify-between items-center">
        <span class="font-accent text-subtle"><%= content_for(:breadcrumb).presence || "~/sre_devlog >" %></span>
        <span class="font-accent text-subtle">uptime: <%= distance_of_time_in_words(Time.new(2025, 12, 25), Time.current) %></span>
      </div>
    </footer>
  </body>
</html>
